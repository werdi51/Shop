<Page x:Class="Pract_SQL3.Pages.SellerPagr" 
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
      xmlns:local="clr-namespace:Pract_SQL3.Pages" 
      mc:Ignorable="d"  
      d:DesignHeight="450" d:DesignWidth="800" 
      Title="SellerPagr" Background="White" 
      IsVisibleChanged="Page_IsVisibleChanged">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Background="#F3F3F3">
            <TextBlock Text="Панель управления заказами" VerticalAlignment="Center" FontSize="20" Margin="20,0"/>
            <Button Content="Выход" Click="Exit" Width="80" Height="30" Margin="20,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Grid.RowSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="Новые заказы" Margin="5" FontSize="16">
                <ListView x:Name="NewOrderList" MouseDoubleClick="OrderList_MouseDoubleClick">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="5" Padding="5" CornerRadius="5">
                                <StackPanel>
                                    <TextBlock Text="{Binding Id, StringFormat='Заказ №{0}'}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding TotalPrice, StringFormat='Сумма: {0} руб.'}"/>
                                    <TextBlock Text="{Binding User.Fio, StringFormat='Клиент: {0}'}" FontSize="10" />
                                    <Button Content="Изменить статус" Click="Button_Click"></Button>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </GroupBox>

            <GroupBox Grid.Column="1" Header="Выполняются" Margin="5" FontSize="16">
                <ListView x:Name="WorkOrderList" MouseDoubleClick="OrderList_MouseDoubleClick">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border  Margin="5" Padding="5" CornerRadius="5">
                                <StackPanel>
                                    <TextBlock Text="{Binding Id, StringFormat='Заказ №{0}'}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding TotalPrice, StringFormat='Сумма: {0} руб.'}"/>
                                    <Button Content="Изменить статус" Click="Button_Click"></Button>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </GroupBox>

            <GroupBox Grid.Column="2" Header="Выполненные" Margin="5" FontSize="16">
                <ListView x:Name="CompleteOrderList" MouseDoubleClick="OrderList_MouseDoubleClick">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="5" Padding="5" CornerRadius="5">
                                <StackPanel>
                                    <TextBlock Text="{Binding Id, StringFormat='Заказ №{0}'}" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding TotalPrice, StringFormat='Сумма: {0} руб.'}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </GroupBox>
        </Grid>
    </Grid>
</Page>